name: Daily Data Update

on:
  schedule:
    - cron: '0 6 * * *'  # 6 AM UTC daily
  workflow_dispatch:

jobs:
  update-data:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v2
      with:
        fetch-depth: 0  # This ensures we have the full history
    
    - name: Set up R
      uses: r-lib/actions/setup-r@v2
      
    - name: Install packages
      run: |
        R -e 'install.packages(c("lubridate", "tidyverse", "janitor", "nanoparquet"))'
        
    - name: Run update script
      run: Rscript notebooks/2024_defender-and-shot-dashboards-import.R
        
    - name: Commit and push if changed
      run: |
        git config --local user.email "actions@github.com"
        git config --local user.name "GitHub Actions"
        git add data/*.parquet
        git commit -m "Update data files" || exit 0
        git push